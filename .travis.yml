language: rust
rust:
    - nightly
env:
    - TRAVIS_NODE_VERSION="10"
os: osx
before_install: 
    - brew install yarn
install: 
    - yarn global add neon-cli
    - yarn
before_script: neon build --release
script: node -e lib/index.js